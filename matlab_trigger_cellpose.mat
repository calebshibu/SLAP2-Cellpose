% Define input and output paths
input = 'F:/tmp/cellpose/refstack_FOV1_20240408_121046_DMD1-REFERENCE.tif';
output = 'F:/tmp/cellpose/output';

% Define the path to the GitHub project
path = 'C:\Users\ScanImage\Documents\GitHub\SLAP2-Cellpose';

% Define the path to the Python virtual environment activation script
python_env_path = 'C:\Users\ScanImage\Documents\GitHub\SLAP2-Cellpose\cellpose';

% Change directory to the GitHub project folder
cd(path);

% Construct the command to activate the virtual environment and run the Python script
command = sprintf('"%s\\Scripts\\activate" && python "%s\\run.py" --input "%s" --output "%s"', python_env_path, path, input, output);

% Call the system command
status = system(command);
